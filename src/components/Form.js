import React from 'react'

function Form() {
    return (
        <div>
            <form>
                    <input type="text" placeholder="Find recipes" />
                    <button>Search</button>
            </form>
        </div>
    )
}

export default Form
